//! Project seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial Project data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PROJECT SEEDER
// ============================================================================

/// Seeder for project
pub struct SeedProjectSeeder;

impl SeedProjectSeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedProjectSeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedProjectSeeder {
    fn name(&self) -> &'static str {
        "SeedProjectSeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if project.projects table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM project.projects")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/project_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM project.projects")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>
